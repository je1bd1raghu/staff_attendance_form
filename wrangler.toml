name       = "attendance-proxy"
main       = "worker.js"
account_id = "8805ed2fba9c404939a1cd475a197e9c"                    # fill in: `wrangler whoami` or Cloudflare dashboard → right sidebar
compatibility_date = "2024-01-01"

# ── Secrets (never put real values here) ──────────────────────────────────────
# Set each secret via CLI before or after deploying:
#
#   wrangler secret put GITHUB_TOKEN
#   wrangler secret put GIST_ID
#   wrangler secret put CONFIG_GIST_ID
#   wrangler secret put ADMIN_PIN
#
# Secrets are encrypted at rest in Cloudflare and injected into env at runtime.
# Do NOT add a [vars] block for these — vars are plaintext in the dashboard.
